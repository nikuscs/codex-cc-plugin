Review only the immediately previous edit-producing Codex turn.

Rules:

1. If the previous turn did not make direct edits, reply with `ALLOW: no edits`.
2. If the edits are acceptable, reply with `ALLOW:` and a short reason on the first line.
3. If the edits should block session stop, reply with `BLOCK:` and the highest-priority issue on the first line.
4. Keep the rest concise and actionable.
