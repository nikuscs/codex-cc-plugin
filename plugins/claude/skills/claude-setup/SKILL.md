---
name: claude-setup
description: Run Claude companion setup checks for the current repository and report Claude CLI availability, auth state, and runtime configuration.
---

# Claude Setup

Run the Claude companion setup flow for the current repository.

Use:

```bash
node plugins/claude/scripts/claude-companion.mjs setup
```

Add `--json` for machine-readable output.
